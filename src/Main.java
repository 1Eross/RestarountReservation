import java.io.IOException;
import java.util.Scanner;


public class Main {

    public static void main(String[] args) throws IOException {
        /*IOEllements.IO.Log.Clear();
        LogicalParts.Database database = new LogicalParts.Database("login.txt", "clients.txt", null, "tables.txt");
        Node temp = LogicalParts.Registration.Authorization(database.loginsBuffer, database.clientsBuffer);
        if (temp != null) {
            JOptionPane.showMessageDialog(null, "Пользователь " + temp.dataline);
        } else {
            JOptionPane.showMessageDialog(null, "Пользователь не найден");
        }*/

    }
}